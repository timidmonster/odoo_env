<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="s_call.list">
      <field name="name">sCall list</field>
      <field name="model">s_call.s_call</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="s_call.action_window">
      <field name="name">sCall window</field>
      <field name="res_model">s_call.s_call</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="s_call.action_server">
      <field name="name">sCall server</field>
      <field name="model_id" ref="model_s_call_s_call"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sCall" id="s_call.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="s_call.menu_1" parent="s_call.menu_root"/>
    <menuitem name="Menu 2" id="s_call.menu_2" parent="s_call.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="s_call.menu_1_list" parent="s_call.menu_1"
              action="s_call.action_window"/>
    <menuitem name="Server to list" id="s_call" parent="s_call.menu_2"
              action="s_call.action_server"/>
    -->
  </data>
</openerp>